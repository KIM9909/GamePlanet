// PlayInfo.jsx의 상태 관리
import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const CatchMindSlice = createSlice({
  name: "draw",
  initialState,
  reducers: {},
  extraReducers: (builder) => {},
});

export const {} = CatchMindSlice.actions;

export default CatchMindSlice.reducer;
